# Example environment variables for Logistics Integration Co-pilot
# Copy to .env and adjust values. Do NOT commit real secrets.

# Public (only for local dev experimentation; remove in production builds)
VITE_GEMINI_API_KEY=YOUR_DEV_ONLY_GEMINI_KEY

# Server-side only (used by the proxy in server/index.js if implemented)
GEMINI_API_KEY=YOUR_SECURE_SERVER_KEY

# Optional: override port
PORT=5173

